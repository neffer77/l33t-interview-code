(function(C){'use strict';const VERSION=1;const REQUIRED=['AgeCurriculumPools','TownCenterAdvancement','AgeVisualEvolution','DistrictAgeEvolution','AgeUnlockLandmarks','AgeTransitionReveal'];function systems(){return Object.fromEntries(REQUIRED.map(k=>[k,!!C[k]]))}function score(state,world){const sys=systems(),coverage=Object.values(sys).filter(Boolean).length/REQUIRED.length;const age=C.AgeProgression?.snapshot?.(state,world),land=C.AgeUnlockLandmarks?.snapshot?.(state,world),pool=C.AgeCurriculumPools?.pool?.(state),districts=C.CurriculumDistricts?.summary?.(world,state)||{};const districtLevels=Object.values(districts).map(x=>Number(x?.maturity?.level)||0),districtAvg=districtLevels.length?districtLevels.reduce((a,b)=>a+b,0)/districtLevels.length:0;const checks={coverage:coverage===1,ageState:!!age?.current,curriculumAligned:!!pool&&Number(age?.current?.level||state?.eraLevel||1)===Number(C.AgeCurriculumPools?.level?.(state)||1),landmarksBounded:!land||land.landmarks.length<=6,transitionRewardsBounded:Math.max(...Object.values(C.AgeTransitionReveal?.REWARDS||{0:0}).map(v=>Object.values(v||{}).reduce((a,b)=>a+(Number(b)||0),0)))<=36,districtMaturityBounded:districtAvg<=5};const ready=Object.values(checks).every(Boolean);return{version:VERSION,ready,coverage:Number((coverage*100).toFixed(1)),systems:sys,checks,currentAge:age?.current||null,curriculum:pool||null,districtAverage:Number(districtAvg.toFixed(2)),landmarks:land?.landmarks||[],next:ready?'Phase 4 complete — proceed to P5 Living City.':'Resolve Phase 4 integration gaps before P5.'}}function install(state,world){if(!state)return false;C.events?.emit?.('p4:integration',score(state,world));return true}C.P4Integration={VERSION,REQUIRED,systems,score,install};})(window.Codeopolis);