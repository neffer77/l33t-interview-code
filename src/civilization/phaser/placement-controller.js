(function(C){
  'use strict';
  class PlacementController{
    constructor(scene,world){this.scene=scene;this.world=world;this.iso=C.PixelWorldProjection;this.layout=scene.layout||this.iso.layout(world.world.width,world.world.height);this.preview=null;this.down=null;this.lastTile=null;this.hud=null;this.bind();this.installHud();this.syncTool()}
    bind(){const input=this.scene.input;input.on('pointerdown',p=>{this.down={id:p.id,x:p.x,y:p.y,moved:false}});input.on('pointermove',p=>{if(this.down&&this.down.id===p.id&&Math.abs(p.x-this.down.x)+Math.abs(p.y-this.down.y)>8)this.down.moved=true;this.updatePreview(p)});input.on('pointerup',p=>{const tap=this.down&&this.down.id===p.id&&!this.down.moved;this.down=null;if(tap)this.confirm(p)});C.events.on('world:tool',()=>this.syncTool());C.events.on('world:building-placed',()=>this.clearPreview());C.events.on('world:building-unplaced',()=>this.refreshPreview());C.events.on('placement:rejected',e=>this.setHudStatus(e.reason,false))}
    tool(){return this.world.world.tool||{mode:'inspect',buildingId:null}}
    haptic(ms=10){try{navigator.vibrate?.(ms)}catch{}}
    installHud(){const host=this.scene.game?.canvas?.parentElement;if(!host||host.querySelector('.p1-placement-hud'))return;host.style.position=host.style.position||'relative';const hud=document.createElement('div');hud.className='p1-placement-hud';hud.style.cssText='position:absolute;left:10px;right:10px;bottom:10px;z-index:30;display:none;align-items:center;justify-content:space-between;gap:10px;padding:9px 10px;background:rgba(13,28,35,.92);border:1px solid rgba(255,255,255,.16);border-radius:10px;color:#fff;font:12px system-ui;backdrop-filter:blur(8px);pointer-events:auto';hud.innerHTML='<div><b data-placement-title>Place building</b><div data-placement-status style="opacity:.75;margin-top:2px">Move over the map, then tap a green footprint.</div></div><button type="button" data-placement-cancel style="min-width:72px;min-height:42px;border-radius:8px;border:1px solid rgba(255,255,255,.2);background:#243944;color:#fff;font-weight:800">Cancel</button>';host.appendChild(hud);hud.querySelector('[data-placement-cancel]').onclick=()=>this.cancel();this.hud=hud}
    cancel(){const moved=this.world.ensureEditState?.()?.relocation;if(moved)this.world.cancelRelocation?.();else this.world.setTool('inspect');this.clearPreview();this.haptic();try{typeof persist==='function'&&persist(false)}catch{}if(moved){this.scene.refresh?.();C.phaserCity?.catalog?.notify?.('↩️ Move cancelled · building restored')}}
    setHudStatus(text,ok=true){if(!this.hud)return;const el=this.hud.querySelector('[data-placement-status]');if(el){el.textContent=text;el.style.color=ok?'#b8f6ca':'#ffb5b5'}}
    syncTool(){const tool=this.tool(),placing=tool.mode==='building'&&tool.buildingId;if(this.hud)this.hud.style.display=placing?'flex':'none';if(placing){const def=C.BuildingRegistry.definition(this.world,tool.buildingId),title=this.hud?.querySelector('[data-placement-title]'),moving=!!this.world.ensureEditState?.()?.relocation;if(title)title.textContent=`${moving?'↔️ Move':'🏗️ Place'} ${def.name} · ${def.footprint.w}×${def.footprint.h}`;this.setHudStatus(moving?'Tap a green footprint. Cancel restores the original location.':'Move over the map, then tap a green footprint.',true);try{if(typeof switchTab==='function')switchTab('city')}catch{}requestAnimationFrame(()=>{const shell=document.querySelector('#codeopolisIonicShell');if(shell)shell.dataset.view='city'});this.refreshPreview()}else this.clearPreview()}
    pointerTile(p){const wp=p.positionToCamera(this.scene.cameras.main),q=this.scene.fromWorld?this.scene.fromWorld(wp.x,wp.y):this.iso.fromWorld(wp.x,wp.y,this.layout);return q}
    updatePreview(p){const tool=this.tool();if(tool.mode!=='building'||!tool.buildingId){this.clearPreview();return}const tile=this.pointerTile(p);if(this.lastTile&&tile.x===this.lastTile.x&&tile.y===this.lastTile.y)return;this.lastTile=tile;this.drawPreview(tile.x,tile.y,tool.buildingId)}
    refreshPreview(){if(this.tool().mode!=='building')this.clearPreview();else if(this.lastTile)this.drawPreview(this.lastTile.x,this.lastTile.y,this.tool().buildingId)}
    clearPreview(){this.preview?.destroy?.();this.preview=null;this.lastTile=null}
    drawPreview(x,y,id){this.preview?.destroy?.();const verdict=this.world.canPlaceBuilding(id,x,y),def=C.BuildingRegistry.definition(this.world,id),g=this.scene.add.graphics().setDepth(20000),ok=verdict.ok,color=ok?0x76e39a:0xff6b6b,cells=C.BuildingRegistry.cells(def,x,y);g.fillStyle(color,.24);g.lineStyle(3,color,.96);for(const c of cells){const pts=(this.scene.tilePolygon?this.scene.tilePolygon(c.x,c.y):this.iso.corners(c.x,c.y,this.layout));g.fillPoints(pts,true);g.strokePoints(pts,true)}const center=this.iso.footprintCenter(def,x,y,this.layout),label=this.scene.add.text(center.x,center.y-26,ok?`${def.icon} ${def.name}`:`✕ ${verdict.reason}`,{fontFamily:'monospace',fontSize:'10px',color:ok?'#d9ffe6':'#ffd8d8',backgroundColor:'#13202bdd',padding:{x:5,y:3}}).setOrigin(.5,1).setDepth(20001);this.preview=this.scene.add.container(0,0,[g,label]).setDepth(20000);this.setHudStatus(ok?'Tap to construct':verdict.reason,ok)}
    confirm(p){const tool=this.tool();if(tool.mode!=='building'||!tool.buildingId)return;const tile=this.pointerTile(p),moving=!!this.world.ensureEditState?.()?.relocation,r=this.world.placeBuilding(tool.buildingId,tile.x,tile.y);if(!r.ok){this.drawPreview(tile.x,tile.y,tool.buildingId);this.haptic(18);C.events.emit('placement:rejected',{...tile,id:tool.buildingId,reason:r.reason});return}if(moving)this.world.finishRelocation?.();this.world.setTool('inspect');this.world.select(tile.x,tile.y);this.haptic(14);try{typeof persist==='function'&&persist(false)}catch{}try{typeof render==='function'&&render()}catch{}const def=C.BuildingRegistry.definition(this.world,tool.buildingId),center=this.iso.footprintCenter(def,tile.x,tile.y,this.layout);this.scene.cameras.main.pan(center.x,center.y,420,'Sine.easeInOut');C.events.emit('placement:confirmed',{...tile,id:tool.buildingId,footprint:r.footprint,moved:moving});this.clearPreview()}
  }
  C.PlacementController=PlacementController;
})(window.Codeopolis);
