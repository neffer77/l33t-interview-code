(function(C){'use strict';const VERSION=1;const REQUIRED=['LivingCityCitizens','CitizenSchedules','AmbientCityActivity','CityEventReactions','CitizenIdentities','CitizenDialogueMentorship'];function systems(){return Object.fromEntries(REQUIRED.map(k=>[k,!!C[k]]))}function score(state,world){const sys=systems(),coverage=Object.values(sys).filter(Boolean).length/REQUIRED.length,citizens=world?.citizens||state?.livingCity?.citizens||[],count=Array.isArray(citizens)?citizens.length:0,dialogue=state?.livingCityDialogue||null;const checks={coverage:coverage===1,citizenPopulationBounded:count<=120,dialogueCountsBounded:!dialogue||Object.values(dialogue.counts||{}).every(v=>(Number(v)||0)<=10000),eventCatalogPresent:!!C.CityEventReactions,identitySystemPresent:!!C.CitizenIdentities,mentorshipPresent:!!C.CitizenDialogueMentorship};const ready=Object.values(checks).every(Boolean);return{version:VERSION,ready,coverage:Number((coverage*100).toFixed(1)),systems:sys,checks,citizenCount:count,next:ready?'Phase 5 complete — proceed to P6 Existing Systems Integration.':'Resolve Phase 5 Living City integration gaps before P6.'}}function emit(state,world){const out=score(state,world);C.events?.emit?.('p5:integration',out);return out}function install(state,world){if(!state)return false;emit(state,world);return true}C.P5Integration={VERSION,REQUIRED,systems,score,emit,install};})(window.Codeopolis);
