(function(C){'use strict';function boot(){const g=C.game;if(!g||g.phase26)return;const G=C.get('SkillGraph'),U=C.get('Phase26UI');if(!G||!U)return;const graph=new G(),ui=new U(g,graph);g.phase26={graph,ui};ui.refresh();window.addEventListener('codeopolis:mastery-earned',e=>{const id=e.detail?.challengeId||e.detail?.id||e.detail?.challenge?.id;if(id){graph.record(id);ui.refresh()}});window.addEventListener('codeopolis:repo-review-complete',()=>ui.refresh());window.addEventListener('codeopolis:project-completed',()=>ui.refresh())}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>setTimeout(boot,1000));else setTimeout(boot,1000)})(window.Codeopolis);