import fs from 'node:fs';import vm from 'node:vm';import assert from 'node:assert/strict';
const handlers={};const C={events:{on:(e,fn)=>handlers[e]=fn,emit(){}}};const ctx={window:{Codeopolis:C,addEventListener(){}},document:undefined,setTimeout:fn=>{fn();return 0}};vm.createContext(ctx);vm.runInContext(fs.readFileSync('src/integration/existing-system-world-venues.js','utf8'),ctx);assert.equal(C.ExistingSystemWorldVenues.VERSION,1);assert.equal(C.ExistingSystemWorldVenues.VENUES.length,5);assert.equal(C.ExistingSystemWorldVenues.venueFor('career').id,'interview-academy');assert.equal(C.ExistingSystemWorldVenues.venueFor('company').id,'company-campus');assert.equal(C.ExistingSystemWorldVenues.venueFor('social').id,'team-commons');assert.equal(C.ExistingSystemWorldVenues.venueFor('world').id,'great-works');assert.equal(C.ExistingSystemWorldVenues.venueFor('other','codeopolis:repo-review-complete').id,'repository-guild');assert.equal(C.ExistingSystemWorldVenues.install(),true);assert.ok(handlers['existing-system:activity']);assert.ok(handlers['civilization:crisis']);console.log('P6-L existing systems world venues: ok');
