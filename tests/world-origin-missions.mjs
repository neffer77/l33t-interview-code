import fs from 'node:fs';import vm from 'node:vm';import assert from 'node:assert/strict';
const emitted=[];const C={events:{emit:(e,p)=>emitted.push([e,p])}};const ctx={window:{Codeopolis:C},Date,setTimeout:()=>0};vm.createContext(ctx);vm.runInContext(fs.readFileSync('src/civilization/world-origin-missions.js','utf8'),ctx);const state={};let active=C.WorldOriginMissions.begin(state,{kind:'building',id:'market',name:'Hash Market',challengeId:'two-sum',requirement:'Restore fast matching'},'market');assert.equal(active.origin.kind,'building');C.WorldOriginMissions.trainingStarted(state,{buildingId:'market',challenge:{id:'two-sum',title:'Two Sum'},requirement:{text:'Restore fast matching'}});const card=C.WorldOriginMissions.missionCard(state);assert.match(card.title,/Hash Market/);assert.match(card.objective,/Two Sum/);assert.match(card.impact,/advance Hash Market/);const done=C.WorldOriginMissions.completion(state,{challengeId:'two-sum',building:{name:'Hash Market'},progress:2,unlocked:false});assert.equal(done.progress,2);assert.equal(state.worldOriginMissions.active,null);assert.equal(state.worldOriginMissions.history.length,1);assert.ok(emitted.some(([e])=>e==='world-mission:origin-set'));assert.ok(emitted.some(([e])=>e==='world-mission:completed'));const npc=C.WorldOriginMissions.originFromModel({kind:'citizen',citizenId:'ada',name:'Ada',role:'Researcher',body:'Need graph help',concept:'graphs'},'transit');assert.equal(npc.name,'Ada');assert.equal(npc.placeId,'transit');console.log('P6-K world-origin missions: ok');
