import fs from 'node:fs';import vm from 'node:vm';import assert from 'node:assert/strict';const C={AgeProgression:{current:s=>({level:s.eraLevel||1})},CurriculumDistricts:{summary:()=>({materials:{maturity:{level:1}},trade:{maturity:{level:2}},research:{maturity:{level:3}},infrastructure:{maturity:{level:3}},stability:{maturity:{level:4}}})},events:{emit(){}}};const ctx={window:{Codeopolis:C,state:{}},console};vm.createContext(ctx);vm.runInContext(fs.readFileSync('src/progression/age-unlock-landmarks.js','utf8'),ctx);const A=C.AgeUnlockLandmarks;assert.equal(A.LANDMARKS.length,6);assert.equal(A.status({eraLevel:1},{},A.LANDMARKS[0]).unlocked,true);assert.equal(A.status({eraLevel:1},{},A.LANDMARKS[1]).unlocked,false);assert.equal(A.status({eraLevel:2},{},A.LANDMARKS[1]).unlocked,true);assert.equal(A.buildingGate({eraLevel:1},{requiresEra:2}).unlocked,false);assert.equal(A.buildingGate({eraLevel:2},{requiresEra:2}).unlocked,true);assert.equal(A.available({eraLevel:3},{}).length,3);console.log('P4-E age unlock landmarks: ok');