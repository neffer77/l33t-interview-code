import fs from 'node:fs';import vm from 'node:vm';import assert from 'node:assert/strict';
const emitted=[];const mkImage=()=>({tint:null,scale:null,setTint(v){this.tint=v;return this},setScale(v){this.scale=v;return this}});const image=mkImage();const objects=[];const scene={tile:32,snapshot:{buildings:[{x:1,y:2,id:'lab'}]},buildingRefs:new Map([['1,2',{image}]]),__ageVisualDecor:[],cameras:{main:{setBackgroundColor(v){scene.bg=v}}},scale:{width:360},add:{rectangle(...a){const o={kind:'rect',a,setDepth(){return this},destroy(){this.dead=true}};objects.push(o);return o},triangle(...a){const o={kind:'tri',a,setDepth(){return this},destroy(){this.dead=true}};objects.push(o);return o},circle(...a){const o={kind:'circle',a,setDepth(){return this},destroy(){this.dead=true}};objects.push(o);return o},text(...a){const o={kind:'text',a,setScrollFactor(){return this},setDepth(){return this},destroy(){this.dead=true}};objects.push(o);return o}},tweens:{add(){}}};
const C={events:{emit(n,p){emitted.push([n,p])},on(){}},AgeProgression:{current:s=>({level:s.eraLevel})},phaserCity:{game:{scene:{getScene:()=>scene}}}};const ctx={window:{Codeopolis:C},setTimeout,console};vm.createContext(ctx);vm.runInContext(fs.readFileSync('src/progression/age-visual-evolution.js','utf8'),ctx);const V=C.AgeVisualEvolution;
assert.notEqual(V.profile(1).background,V.profile(2).background);assert.notEqual(V.profile(1).ornament,V.profile(2).ornament);assert.ok(V.profile(2).scale>V.profile(1).scale);const out=V.apply({eraLevel:2},scene);assert.equal(out.ok,true);assert.equal(out.level,2);assert.equal(scene.bg,V.profile(2).background);assert.equal(image.tint,V.profile(2).tint);assert.equal(image.scale,V.profile(2).scale);assert.ok(out.decorations>=2);assert.ok(emitted.some(([n])=>n==='age-visual:applied'));
console.log('P4-C age visual evolution: ok');
