import fs from 'node:fs';import vm from 'node:vm';import assert from 'node:assert/strict';
const C={};const ctx={window:{Codeopolis:C}};vm.createContext(ctx);vm.runInContext(fs.readFileSync('src/integration/civilization-status-dashboard.js','utf8'),ctx);const D=C.CivilizationStatusDashboard;
const state={careerCity:{prestige:40,opportunity:60,confidence:80,recruitingVisibility:50,lastEvent:{event:'job:offer'}},companyCity:{culture:30,execution:60,influence:90,alignment:60},socialTeamCity:{cohesion:25,mentorship:50,collaboration:75,momentum:100,synergy:63},worldStoryCity:{resilience:20,legacy:40,narrative:60,stability:80,continuity:50}};
const snap=D.snapshot(state);assert.equal(snap.domains.length,4);assert.equal(snap.domains[0].score,50);assert.equal(snap.domains[1].score,60);assert.equal(snap.domains[2].score,63);assert.equal(snap.domains[3].score,50);assert.equal(snap.overall,56);assert.equal(D.level(0),'quiet');assert.equal(D.level(20),'emerging');assert.equal(D.level(50),'developing');assert.equal(D.level(70),'strong');assert.equal(D.level(90),'thriving');assert.equal(D.snapshot({}).overall,0);assert.equal(D.clamp(150),100);assert.equal(D.clamp(-5),0);console.log('P6-F unified civilization status dashboard: ok');
