import fs from 'node:fs';import vm from 'node:vm';import assert from 'node:assert/strict';
const handlers={};const calls=[];const C={events:{on:(e,f)=>handlers[e]=f},phaserCity:{game:{scene:{getScene:()=>({tile:32,adapter:{snapshot:()=>({width:12,height:8})},cameras:{main:{pan:(...a)=>calls.push(['pan',...a]),shake:(...a)=>calls.push(['shake',...a])}},add:{circle:()=>({setDepth(){return this},destroy(){}}),text:()=>({setOrigin(){return this},setDepth(){return this},destroy(){}})},tweens:{add:o=>{calls.push(['tween']);o.onComplete?.()}},buildingRefs:new Map()})}}}};const ctx={window:{Codeopolis:C},Date};vm.createContext(ctx);vm.runInContext(fs.readFileSync('src/integration/city-game-feel.js','utf8'),ctx);assert.equal(C.CityGameFeel.VERSION,1);assert.equal(C.CityGameFeel.install(),true);assert.ok(handlers['city-growth:started']);assert.ok(handlers['learning-city:visible-consequence']);assert.ok(handlers['civilization:crisis']);handlers['city-growth:started']({x:2,y:3,kind:'construction'});assert.ok(calls.some(x=>x[0]==='pan'));handlers['civilization:crisis']({});assert.ok(calls.some(x=>x[0]==='shake'));console.log('P6-M visual and sensory game feel: ok');
