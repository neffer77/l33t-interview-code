import fs from 'node:fs';import vm from 'node:vm';import assert from 'node:assert/strict';
const listeners={},emitted=[];const C={AdaptiveChallengeSelector:{},InterviewReadiness:{},CompanyLife:{},TeamMissions:{},CivilizationLegacy:{},events:{on:(e,f)=>{listeners[e]=f},emit:(e,p)=>emitted.push([e,p])}};const ctx={window:{Codeopolis:C}};vm.createContext(ctx);vm.runInContext(fs.readFileSync('src/integration/existing-systems-bridge.js','utf8'),ctx);const B=C.ExistingSystemsBridge;const cov=B.coverage();assert.equal(cov.domains.learning.includes('AdaptiveChallengeSelector'),true);assert.equal(cov.domains.company.includes('CompanyLife'),true);assert.equal(B.normalize('team:mission',{missionId:'m1'}).domain,'social');B.install();assert.equal(typeof listeners['team:mission'],'function');listeners['team:mission']({missionId:'m1',title:'Ship feature'});const activity=emitted.find(([e])=>e==='existing-system:activity');assert.equal(activity[1].domain,'social');assert.equal(activity[1].id,'m1');assert.equal(emitted.some(([e])=>e==='existing-system:social'),true);console.log('P6-A existing systems registry and event bridge: ok');
