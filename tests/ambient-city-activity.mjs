import fs from 'node:fs';import vm from 'node:vm';import assert from 'node:assert/strict';
const C={};const ctx={window:{Codeopolis:C}};vm.createContext(ctx);vm.runInContext(fs.readFileSync('src/civilization/phaser/ambient-city-activity.js','utf8'),ctx);const A=C.AmbientCityActivity;
assert.equal(A.descriptor({x:1,y:2,district:'materials',progress:1},7).kind,'smoke');assert.equal(A.descriptor({x:1,y:2,district:'research',progress:1},7).kind,'spark');assert.equal(A.descriptor({x:1,y:2,district:'trade',progress:1},7).kind,'market');assert.equal(A.descriptor({x:1,y:2,district:'infrastructure',progress:1},7).kind,'maintenance');assert.equal(A.descriptor({x:1,y:2,district:'research',progress:.5},7).kind,'construction');assert.equal(A.descriptor({x:1,y:2,district:'research',progress:1},7).phase,A.descriptor({x:1,y:2,district:'research',progress:1},7).phase);assert.equal(A.forSnapshot({seed:9,buildings:[{id:'lab',x:3,y:4,district:'research',progress:1}]}).length,1);console.log('P5-C ambient city activity and particles: ok');
