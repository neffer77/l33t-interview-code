import fs from 'node:fs';import vm from 'node:vm';import assert from 'node:assert/strict';
const C={WorldFirstUX:{session:{cityEntered:true,interacted:true,missionAccepted:true,solved:true,consequence:true},acceptance:(state,world,journey)=>({ready:['cityEntered','interacted','missionAccepted','solved','consequence'].every(k=>!!journey[k]),checks:{journeyComplete:true}})},LearningCityLoop:{},CodingRewardPipeline:{},WorldInteractions:{},WorldOriginMissions:{},SimCityGrowthLoop:{},ExistingSystemWorldVenues:{},CityGameFeel:{},phaserCity:{},events:{on(){},emit(){}}};const ctx={window:{Codeopolis:C},setTimeout};vm.createContext(ctx);vm.runInContext(fs.readFileSync('src/quality/p7-release-readiness.js','utf8'),ctx);const R=C.P7ReleaseReadiness;const state={population:8,money:100,research:5,xp:12,worldOriginMissions:{history:Array(3).fill({})}},world={placedBuildings:()=>[{},{},{}],roadTiles:()=>[{},{},{},{}]};let report=R.evaluate(state,world,C.WorldFirstUX.session);assert.equal(report.ready,true);assert.equal(report.score,100);assert.equal(report.blockers.length,0);report=R.evaluate(state,world,{cityEntered:true,interacted:true,missionAccepted:false,solved:false,consequence:false});assert.equal(report.ready,false);assert.equal(report.checks.p6Acceptance,false);assert.equal(report.blockers.some(x=>x.includes('world → interaction')),true);delete C.CityGameFeel;report=R.evaluate(state,world,C.WorldFirstUX.session);assert.equal(report.ready,false);assert.equal(report.modules.CityGameFeel,false);assert.equal(report.blockers.some(x=>x.includes('CityGameFeel')),true);C.CityGameFeel={};report=R.evaluate({...state,population:-1},world,C.WorldFirstUX.session);assert.equal(report.ready,false);assert.equal(report.stateSafety.population,false);assert.equal(report.blockers.some(x=>x.includes('population')),true);const longState={...state,worldOriginMissions:{history:Array(25).fill({})}};report=R.evaluate(longState,world,C.WorldFirstUX.session);assert.equal(report.stateSafety.missionHistoryBounded,false);console.log('P7-A release readiness and full-loop playtest gate: ok');
